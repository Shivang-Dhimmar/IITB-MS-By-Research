/*
#include <simplecpp>
main_program{
    int nSides;
    cout<< "Enter the number of sides in polygon:";
    cin>> nSides;

    if(nSides>2){
        turtleSim();
        repeat(nSides){
            forward(2);
            left(360.0/nSides);
        }
        wait(2);
    }
    else{
        cout<< "Enter Valid Number!";
        wait(2);
    }
}

*/
